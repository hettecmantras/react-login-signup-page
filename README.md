# React Login & Sign Up Page

A polished static UI that toggles between login and signup forms. Built with React and styled to be responsive without touching a backend.

## Setup

```bash
cd frontend
npm install
npm start
```

## Scripts

- `npm run build`: Bundle the app for production
- `npm test`: Run the included React tests
- `npm run eject`: Eject from the Create React App configuration when needed
